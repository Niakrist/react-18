1. Batching - с React18+ асинхронный. API для группировки (батчинга) нескольких обновлений состояния в один рендер (Объединение state в один re-render)
2. unstable_batchedUpdate - API для группировки (батчинга) нескольких обновлений состояния в один рендер для старых версий React
3. Concurrent Mode
4. Current Tree и Work-in-Progress Tree - Это два VDOM дерева
5. Current Tree, это то, что видит пользователь и он равен реальному DOM дереву
6. Work-in-Progress Tree, это скрытый от глаз процесс при котром React проводит свои вычисления и когда они закончатся, он будет сравнить Current Tree и Work-in-Progress Tree, после чего применит для реального DOM только те изменения, где будет разница между деревьями

Два VDOM дерева. React держит в памяти два дерева. Это называется double buffering (двойная буферизация).
Current Tree - VDOM = то, что видит пользователь - DOM. Current Tree — это буквально то, что находится в DOM прямо сейчас.
Work-in-Progress и процесс сравнения
Этап 1: Построение WIP дерева (Render Phase)
React берет current tree и идет по нему, создавая WIP
Прямо в процессе создания WIP, React сразу видит различия
React сразу помечает этот узел как требующий обновления
React рекурсивно обходит все узлы
К концу этого процесса WIP дерево уже содержит все "находки" (effect tags) о том, что менять
Current Tree — это то, что React уже отрисовал в реальный DOM в прошлом коммите. Это "слепок" текущего состояния интерфейса.
Work-in-Progress Tree (Дерево в процессе) — Когда происходит обновление (setState), React создает work-in-progress tree:

Этап 2: Commit Phase. Когда WIP дерево полностью построено, React не "сравнивает" деревья заново. Вместо этого он просто проходит по готовому WIP дереву и применяет все эффекты (effect tags), которые были найдены на этапе Render

Render Phase:

- Берем current дерево
- Клонируем его в WIP
- Проходим по узлам:
  - Видим, что текст изменился
  - Помечаем в WIP: "нужно обновить текст"
- Построили WIP с пометками

Commit Phase:

- Идем по WIP
- Видим пометку "обновить текст"
- Обновляем DOM: div.textContent = "пока"
- Переключаем указатели: WIP становится Current

4. useTransition - когда мы контролируем обновление (setState) и нужно сделать тяжелые обновления низкоприоритетными, чтобы интерфейс оставался отзывчивым.
5. useDeferredValue(props) - когда есть конечный результату вычислений и нужно сделать тяжелые обновления низкоприоритетными, чтобы интерфейс оставался отзывчивым.
   Важно! у useDeferredValue Нет isPending! Приходится придумывать свой флаг

```
const deferredQuery = useDeferredValue(query);
const isStale = query !== deferredQuery;
```

6. flushSync

7. Concurrent Mode действительно работает "под капотом" автоматически, и React сам управляет приоритетами, даже если вы не используете специальные хуки.
   7.1. Автоматическое определение приоритетов (низкий приоритет (асинхронная загрузка), ВЫСОКИЙ приоритет (пользовательский ввод))
   7.2. Автоматический батчинг (группировка). React 18+ автоматически группирует ВСЕ обновления, Будет ОДИН рендер
   7.3. Конкурентный рендеринг "из коробки". Загрузка данных может быть прервана, если пользователь переключится на другую api
   7.4. React может начать рендерить компонент, но прервать, если пришло более важное обновление

### Что React автоматически считает "важным"?

Высокий приоритет (автоматически):
🔴 Клики, нажатия клавиш
🔴 Ввод текста в поля
🔴 Наведение мыши
🔴 Анимации (CSS transitions)
🔴 Обновления внутри обработчиков событий

Низкий приоритет (автоматически):
🔵 Загрузка данных (then/catch/finally)
🔵 Таймауты и интервалы (setTimeout/setInterval)
🔵 Микрозадачи (Promise.then)
🔵 Обновления после асинхронных операций

Осталось:

8. useId - генерирует уникальный id
9. Lazy Loading, codesplit, Suspense для SSR
10. useSyncExternalStore
11. useInsertionEffect
12. createRoot (для Client) и hydrateRoot (для SSR)
13. current tree and

# Render и Commit

- Фазы (Trigger → Render → Commit): Это базовая архитектура React, которая остается верной и сейчас.

- Синхронность Render-а: До появления Concurrent Mode (React 18+) рендер (фаза вычислений) был синхронным. Как только начинался рендер, React рекурсивно «проходил» по всему дереву компонентов (или его части), и его нельзя было прервать.

- Блокировка (Blocking): Пока React выполняет этот синхронный обход (рендер), основной поток занят. Браузер не может отрисовать кадры, обработать следующие клики или ввод текста. Это называется «блокирующий рендеринг».

1. Trigger. Пользователь совершил действие, которое изменило state (клик, набор текста)
2. Render. React собирает все изменения state, на уровне VDOM определяет какие узлы должны измениться. (виртуальный отрисовка)
3. Commit. После того, как все вычисления сделал вносит изменения в реальны DOM. (изменения в DOM)

Эти операции синхронные и если на этапе Render происходят длительные вычисления, то интерфейс не будет реагировать ни на какие тригеры до изменения в DOM, эти тригеры будут попдать в очередь макротаксок. После отрисовки React продолжит их обрабатывать. Поэтому интерфейс будет подлагивать.

# Concurrent Mode

- Фазы (Trigger → Render → Commit)

1. Trigger. Пользователь совершил действие, которое изменило state (клик, набор текста)
2. Render. (Теперь не блокирующая и прерываемая). React обходит VDOM и собирает все изменения state, на уровне VDOM определяет какие узлы должны измениться, но если происходит новый Trigger, то React прерывет или отменяет фазу Render и сначала обрабатывает Trigger, после чего продолжает фазу Render. у Trigger высокий приоритет, у отрисовки или запросов к серверу низкий приоритет.
3. Commit. После того, как все вычисления сделал вносит изменения в реальны DOM.

Высокий приоритет
├── SyncLane (синхронный) - критически важно
├── InputContinuousLane (непрерывный ввод) - печать, движение мыши
├── DefaultLane (обычный) - загрузка данных, обычные обновления
└── IdleLane (фоновый) - prefetching, невидимые обновления
Низкий приоритет

# VDOM
