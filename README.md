1. Batching
2. ConcurrentMode
3. useTransition
4. useDeferredValue
5. flushSync

6. Concurrent Mode действительно работает "под капотом" автоматически, и React сам управляет приоритетами, даже если вы не используете специальные хуки.
   6.1. Автоматическое определение приоритетов (низкий приоритет (асинхронная загрузка), ВЫСОКИЙ приоритет (пользовательский ввод))
   6.2. Автоматический батчинг (группировка). React 18+ автоматически группирует ВСЕ обновления, Будет ОДИН рендер
   6.3. Конкурентный рендеринг "из коробки". Загрузка данных может быть прервана, если пользователь переключится на другую api
   6.4. React может начать рендерить компонент, но прервать, если пришло более важное обновление

### Что React автоматически считает "важным"?

Высокий приоритет (автоматически):
🔴 Клики, нажатия клавиш
🔴 Ввод текста в поля
🔴 Наведение мыши
🔴 Анимации (CSS transitions)
🔴 Обновления внутри обработчиков событий

Низкий приоритет (автоматически):
🔵 Загрузка данных (then/catch/finally)
🔵 Таймауты и интервалы (setTimeout/setInterval)
🔵 Микрозадачи (Promise.then)
🔵 Обновления после асинхронных операций

Осталось:

6. useId - генерирует уникальный id
7. Lazy Loading, codesplit, Suspense для SSR
8. useSyncExternalStore
9. useInsertionEffect
